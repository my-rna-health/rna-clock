#from sklearn.linear_model import ElasticNet

#regr_binary = ElasticNet(random_state = 0)
#regr_binary.fit(X_train_binary, y_train_binary)
#path = regr_binary.fit(X_train, y_train)
#print("Best model scored", regr_binary.score(X_dev_binary, y_dev_binary))
#print("Lambda =", regr_binary.best_lambda_)